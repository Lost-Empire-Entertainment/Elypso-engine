#include "Main.h"
#include <iostream>

int main()
{
    std::cout << "Hello CMake." << std::endl;

    // Wait for user input before exiting
    std::cout << "Press Enter to exit...";
    std::cin.get();

    return 0;
}